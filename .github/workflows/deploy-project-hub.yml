name: Deploy Project Hub

on:
  push:
    branches:
      - main
    paths:
      - index.php
      - frontend/css/home.css
      - frontend/js/home.js
      - frontend/assets/matrix-city-v2.webp
      - frontend/css/powerbi.css
      - frontend/js/powerbi.js
      - api-client.php
      - app.js
      - api/**
      - auth/**
      - login.php
      - login-processa.php
      - cloud/**
      - gov/**
      - invest/**
      - powerbi/**
      - pet/**
      - PROJECT_HUB_VERSION
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: hostinger-project-hub
  cancel-in-progress: false

jobs:
  deploy:
    name: Publicar menu de projetos
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment:
      name: production
      url: https://lemesolucoesemti.com.br/

    steps:
      - name: Baixar versao aprovada
        uses: actions/checkout@v6

      - name: Detectar publicacao FTPS
        id: ftps
        env:
          FTP_SERVER: ${{ secrets.HOSTINGER_FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.HOSTINGER_FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.HOSTINGER_FTP_PASSWORD }}
        shell: bash
        run: |
          set -Eeuo pipefail
          if [[ -n "$FTP_SERVER" && -n "$FTP_USERNAME" && -n "$FTP_PASSWORD" ]]; then
            echo "configured=true" >> "$GITHUB_OUTPUT"
          else
            echo "configured=false" >> "$GITHUB_OUTPUT"
            echo "FTPS nao configurado; aguardando o deploy Git da Hostinger."
          fi

      - name: Preparar cliente FTPS
        if: steps.ftps.outputs.configured == 'true'
        shell: bash
        run: |
          set -Eeuo pipefail
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends lftp

      - name: Publicar aplicacao e dashboard
        if: steps.ftps.outputs.configured == 'true'
        env:
          FTP_SERVER: ${{ secrets.HOSTINGER_FTP_SERVER }}
          FTP_TLS_HOST: srv1543.hstgr.io
          FTP_USERNAME: ${{ secrets.HOSTINGER_FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.HOSTINGER_FTP_PASSWORD }}
        shell: bash
        run: |
          set -Eeuo pipefail

          printf '%s %s\n' "$FTP_SERVER" "$FTP_TLS_HOST" |
            sudo tee -a /etc/hosts >/dev/null

          lftp -u "$FTP_USERNAME","$FTP_PASSWORD" "ftp://$FTP_TLS_HOST" <<'LFTP_COMMANDS'
          set cmd:fail-exit true
          set ftp:ssl-force true
          set ftp:ssl-protect-data true
          set ssl:verify-certificate true
          set net:max-retries 3
          set net:timeout 30
          mkdir -p frontend/css
          mkdir -p frontend/js
          mkdir -p frontend/assets
          mkdir -p cloud
          mkdir -p gov/private
          mkdir -p invest
          mkdir -p powerbi
          mkdir -p api
          mkdir -p pet/api
          mkdir -p pet/frontend/css
          mkdir -p pet/frontend/js
          mkdir -p pet/includes
          put -O / index.php
          put -O / api-client.php
          put -O / app.js
          mirror --reverse --transfer-all --overwrite --verbose --no-perms --no-umask api /api
          put -O / login.php
          put -O / login-processa.php
          put -O / PROJECT_HUB_VERSION
          put -O /auth auth/security.php
          put -O /auth auth/nextcloud-client.php
          put -O /auth auth/.htaccess
          put -O /frontend/css frontend/css/home.css
          put -O /frontend/css frontend/css/powerbi.css
          put -O /frontend/js frontend/js/home.js
          put -O /frontend/js frontend/js/powerbi.js
          put -O /frontend/assets frontend/assets/matrix-city-v2.webp
          mirror --reverse --transfer-all --overwrite --verbose --no-perms --no-umask cloud /cloud
          mirror --reverse --transfer-all --overwrite --verbose --no-perms --no-umask gov /gov
          mirror --reverse --transfer-all --overwrite --verbose --no-perms --no-umask invest /invest
          mirror --reverse --transfer-all --overwrite --verbose --no-perms --no-umask powerbi /powerbi
          put -O /pet pet/index.php
          put -O /pet pet/callback.php
          put -O /pet pet/logout.php
          put -O /pet pet/.htaccess
          put -O /pet/api pet/api/dashboard.php
          put -O /pet/frontend/css pet/frontend/css/app.css
          put -O /pet/frontend/js pet/frontend/js/app.js
          put -O /pet/includes pet/includes/session.php
          put -O /pet/includes pet/includes/client.php
          bye
          LFTP_COMMANDS

      - name: Verificar pagina publicada
        shell: bash
        run: |
          set -Eeuo pipefail
          for attempt in $(seq 1 40); do
            if curl --silent --show-error --fail --max-time 30 \
              https://lemesolucoesemti.com.br/ -o page.html && \
              grep -q "Todos os projetos Leme" page.html && \
              grep -q "Nuvem / Nextcloud" page.html && \
              curl --silent --show-error --fail --max-time 30 \
                https://lemesolucoesemti.com.br/nextcloud/status.php -o nextcloud-status.json && \
              grep -q '"installed":true' nextcloud-status.json && \
              curl --silent --show-error --head --max-time 30 \
                https://lemesolucoesemti.com.br/cloud/ -o cloud.headers && \
              grep -qi "location: /nextcloud/" cloud.headers && \
              curl --silent --show-error --fail --max-time 30 \
                https://lemesolucoesemti.com.br/gov/ -o gov.html && \
              grep -q "Brasil em dados" gov.html && \
              curl --silent --show-error --head --max-time 30 \
                https://lemesolucoesemti.com.br/invest/ -o invest.headers && \
              grep -qi "location: painel.php" invest.headers && \
              curl --silent --show-error --head --max-time 30 \
                https://lemesolucoesemti.com.br/powerbi/ -o powerbi.headers && \
              grep -qi "location: https://lemeinformatica.com.br/pet/sso-start.php?next=powerbi" powerbi.headers && \
              curl --silent --show-error --fail --max-time 30 \
                https://lemesolucoesemti.com.br/pet/frontend/css/app.css -o pet.css && \
              grep -q -- "--green: #1f765d" pet.css && \
              curl --silent --show-error --head --max-time 30 \
                https://lemesolucoesemti.com.br/pet/ -o pet.headers && \
              grep -qi "location: https://lemeinformatica.com.br/pet/sso-start.php" pet.headers; then
              sync_status="$(curl --silent --show-error --output /dev/null --write-out '%{http_code}' --max-time 30 \
                -X POST -H 'Content-Type: application/json' -d '{}' \
                https://lemesolucoesemti.com.br/api/usuarios-sync.php)"
              test "$sync_status" = "401"
              exit 0
            fi
            sleep 5
          done
          exit 1
