# Central de projetos 1.1.0

Data: 05/08/2026

## Entrega

- restaura `gov/`, `invest/` e `powerbi/` como codigo versionado;
- adiciona `cloud/` como entrada monitorada da Nuvem/Nextcloud;
- corrige o retorno do login para o Power BI;
- remove chave fixa da consulta Power Query;
- publica e verifica todas as novas rotas pelo GitHub Actions.

## Nextcloud

O DNS historico `nuvem.lemesolucoesemti.com.br` continua reservado. A
instalacao antiga nao foi sobrescrita: a recuperacao depende da identificacao
do banco, do diretorio de dados e de um backup valido na Hostinger.
